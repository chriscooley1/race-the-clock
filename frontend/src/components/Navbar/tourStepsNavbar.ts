import { Step } from "react-joyride";

export const tourStepsNavbar: Step[] = [
  {
    target: ".navbar", // Target the navbar
    content: "This is the navigation bar. Use it to access different sections.",
    disableBeacon: true,
  },
  // Add more steps as needed
];